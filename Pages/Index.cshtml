@page
@model api_aquaguard_dotnet.Pages.IndexModel
@{
    ViewData["Title"] = "AquaGuard - Home";
}

<h1>@ViewData["Title"]</h1>
<p>Bem-vindo ao AquaGuard!</p>

<!-- Exemplo de TagHelper: link para recarregar a própria página -->
<a asp-page="/Index" class="btn btn-primary">Recarregar</a>

<!-- Link direto para o SwaggerUI -->
<a class="btn btn-outline-secondary" href="/swagger">Ir para Swagger</a>
